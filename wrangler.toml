name = "tg-bot"
main = "src/worker.js"
compatibility_date = "2023-05-18"
keep_vars = true

[placement]
mode = "smart"

# Create a KV namespace and replace the placeholder id before deploying if the
# blacklist should survive Worker restarts and be shared by all instances.
# [[kv_namespaces]]
# binding = "BLOCKLIST"
# id = "replace-with-your-kv-namespace-id"
